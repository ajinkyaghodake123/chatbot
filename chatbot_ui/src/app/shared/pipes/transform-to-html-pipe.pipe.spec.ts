import { TransformToHtmlPipePipe } from './transform-to-html-pipe.pipe';

describe('TransformToHtmlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformToHtmlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
